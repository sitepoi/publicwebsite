# Cache (Section 18)

Implemented in C11: per-node memory cache (config/objects) + ISR/data-cache
tags `site-<folderId>`, `page-<objectId>`, `type-<appId>`, `host-<host>` +
`POST /api/revalidate` purge webhook (shared secret). Provider-independent.
